# Shared helper code

Reusable utilities imported by the notebooks so we don't repeat boilerplate (loading the
sample corpus, embedding helpers, similarity functions, metric implementations).

Each module is introduced by the chapter that first needs it and is kept small, readable,
and well-commented, since this is a teaching repo.
